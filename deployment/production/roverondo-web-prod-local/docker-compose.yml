version: '3'

services:
  roverondo-web:
    container_name: roverondo-web-prod-local
    build:
      context: ../../..
      dockerfile: deployment/production/Dockerfile
    ports:
      - "4200:80"
